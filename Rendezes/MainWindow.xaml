<Window x:Class="SortingAlgorithmComparison.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sorting Algorithm Comparison" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button x:Name="startButton" Content="Start" Click="StartButton_Click" HorizontalAlignment="Center" Margin="0,10"/>

        <TextBlock x:Name="arrayTimeLabel" Grid.Row="1" HorizontalAlignment="Center" Margin="0,10" Text="{Binding ArrayTime}"/>
        <TextBlock x:Name="listTimeLabel" Grid.Row="2" HorizontalAlignment="Center" Margin="0,10" Text="{Binding ListTime}"/>
    </Grid>
</Window>
